export const JobFilters = (props: any) => {
    return <div>
        name
        posted from {">"} till
        status -{">"} live, expired here we will need to add status to ads too on db level
    </div>;
};
